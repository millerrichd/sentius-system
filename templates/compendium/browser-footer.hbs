<div class="flexrow">
    {{~#if displaySelection}}
    <div class="count {{ invalid }}">
        {{{ localize "SENTIUS.CompendiumBrowser.Selection.Label" summary=summary }}}
    </div>
    <button type="submit">
        {{ localize "SENTIUS.CompendiumBrowser.Selection.Select" }}
    </button>
    {{/if~}}
</div>
