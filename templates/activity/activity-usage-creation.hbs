<section>
    {{~#if hasCreation}}
    <fieldset>
        <legend>{{ localize "SENTIUS.USAGE.SECTION.Creation" }}</legend>
        {{#if template}}
            {{ formField template.field name=template.name value=template.value input=inputs.createCheckboxInput }}
        {{/if}}
    </fieldset>
    {{> "sentius.activity-usage-notes" }}
    {{/if~}}
</section>
