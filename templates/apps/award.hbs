<form autocomplete="off">
    <header>{{ localize "SENTIUS.Award.Title" }}</header>
    <div class="distribution">
        <span>{{localize "SENTIUS.Award.Distribution.Split"}}</span>
        <slide-toggle name="each" {{checked each}}></slide-toggle>
        <span>{{localize "SENTIUS.Award.Distribution.Each"}}</span>
    </div>
    {{#unless hideXP}}
    <label class="xp currency-style">
        <span class="roboto-upper">{{localize "SENTIUS.ExperiencePoints"}}</span>
        {{numberInput xp name="xp" class="uninput" min=0 step=1}}
    </label>
    {{/unless}}
    <section class="currency">
        {{#each currency}}
        <label aria-label="{{label}}">
            <i class="currency {{ @key }}" data-tooltip="{{ label }}"></i>
            {{numberInput value name=(concat "amount." @key) class="uninput" min=0 step=1}}
        </label>
        {{/each}}
    </section>
    <div class="destinations">
        {{#if noPrimaryParty}}
        <p class="no-primary-party">{{localize "SENTIUS.Award.NoPrimaryParty"}}</p>
        {{/if}}
        {{#each destinations}}
        <label>
            <input type="checkbox" name="destination.{{doc.id}}" {{checked checked}}>
            {{{icon}}} {{doc.name}}
        </label>
        {{else}}
        <p class="no-destinations">{{localize "SENTIUS.Award.NoDestinations"}}</p>
        {{/each}}
    </div>
    <button type="submit" name="transfer" disabled>
        <i class="fa-solid fa-trophy"></i> {{localize "SENTIUS.Award.Action"}}
    </button>
</form>
